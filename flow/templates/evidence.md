# Evidence — <work-id>: <title>

> Written <YYYY-MM-DD>. What was verified and where the proof is. Point at
> commits, tests and logs; do not paste them.

## What shipped
<!-- Commits and the files they changed, against the spec's acceptance. -->

## Verification
<!-- Each command as run, its result, and what it proves. What was not
     verified, and why. -->

## Remaining limits
<!-- What this package does not cover; known gaps. -->

## Lessons
<!-- Candidates only. Write a row when one of these happened in this
     package, and not otherwise: the user corrected the work; a failure was
     reproduced and its fix verified; the same observation recurred and
     would prevent a future mistake. One row per lesson: a repeat updates
     the existing row. Never a row per session, never a preference guessed
     from one choice.

     status: candidate | accepted | rejected | superseded. A candidate is
     not an instruction and changes nothing. It becomes `accepted` only when
     the user approves it; then write the rule once, in the project's
     conventions (AGENTS.md) or an ADR, record that place under destination,
     and leave this row as the trace. scope is this repository unless the
     user explicitly says otherwise; seeing it in another project is
     evidence, not permission. A lesson that contradicts a standing user
     instruction is flagged for the user, never applied. Superseded rows
     stay, with their evidence. No scores: evidence and status decide. -->

| id | date | trigger (observed fact) | proposed action (generalization) | scope | evidence | status | destination |
|---|---|---|---|---|---|---|---|
