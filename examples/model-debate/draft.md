# Retry queue proposal

C001: Failed work is retried at most twice.

C002: Every retry waits exactly 1000 milliseconds.

C003: Failed work survives a process restart because the queue stores it.

P001: Replace the existing plain status page with an animated dashboard.
